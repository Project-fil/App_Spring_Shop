spring:
  jpa:
    database: H2
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:~/test
    username: sa
    password: sa
    driverClassName: org.h2.Driver